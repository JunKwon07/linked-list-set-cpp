# linked-list-set-cpp
A custom Set data structure implemented in C++ using singly linked lists. Supports insertion, deletion, searching, and set operations such as union and intersection. Applies object-oriented design, copy/move semantics, and careful memory ownership.
## Build
```bash
g++ -std=c++11 linked_list_set.cpp -o linked_list_set
```
